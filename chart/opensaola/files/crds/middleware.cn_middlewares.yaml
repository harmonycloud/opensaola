---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: middlewares.middleware.cn
spec:
  group: middleware.cn
  names:
    kind: Middleware
    listKind: MiddlewareList
    plural: middlewares
    shortNames:
    - mid
    singular: middleware
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels['middleware\.cn/component']
      name: Type
      type: string
    - jsonPath: .metadata.labels['middleware\.cn/packagename']
      name: Package
      type: string
    - jsonPath: .spec.baseline
      name: Baseline
      type: string
    - jsonPath: .status.state
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          Middleware is the Schema for the middlewares API.
          It represents a deployed middleware instance (e.g., MySQL, Redis) managed by the operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MiddlewareSpec defines the desired state of Middleware.
            properties:
              baseline:
                description: Baseline is the name of the MiddlewareBaseline to use
                  as the default template.
                type: string
              configurations:
                description: Configurations is the list of additional configuration
                  resources to create alongside the middleware.
                items:
                  description: Configuration defines a named configuration with raw
                    values.
                  properties:
                    name:
                      description: Name is the unique name identifying this configuration
                        entry.
                      type: string
                    values:
                      description: |-
                        Values holds configuration values as raw JSON, rendered from CUE/Go templates.
                        The structure depends on the Configuration name and target resource type.
                        For ConfigMap targets, this typically contains key-value pairs; for RBAC targets,
                        it contains role/binding specifications.
                        Example (ConfigMap): {"my.cnf": "max_connections=200\ninnodb_buffer_pool_size=1G"}
                        Example (ServiceAccount): {"automountServiceAccountToken": true}
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
              necessary:
                description: |-
                  Necessary holds required parameters for the middleware instance as raw JSON.
                  The exact schema depends on the middleware type defined in the MiddlewareBaseline.
                  Common fields include image, replicas, storage, and resource requirements.
                  Example: {"image": "redis:7.2", "replicas": 3, "storage": "10Gi"}
                type: object
                x-kubernetes-preserve-unknown-fields: true
              operatorBaseline:
                description: OperatorBaseline references the MiddlewareOperatorBaseline
                  that manages the operator for this middleware.
                properties:
                  gvkName:
                    description: GvkName is the GroupVersionKind identifier used to
                      match the operator baseline.
                    type: string
                  name:
                    description: Name is the name of the MiddlewareOperatorBaseline
                      resource.
                    type: string
                type: object
              parameters:
                description: |-
                  Parameters holds user-configurable parameters for the middleware instance as raw JSON.
                  These values are merged with defaults from the MiddlewareBaseline during reconciliation.
                  The schema varies by middleware type; common fields include port, password, and tuning knobs.
                  Example: {"port": 6379, "maxmemory": "256mb", "databases": 16}
                type: object
                x-kubernetes-preserve-unknown-fields: true
              preActions:
                description: PreActions is the list of pre-actions to execute before
                  the main reconciliation workflow.
                items:
                  description: PreAction defines a pre-action to execute before the
                    main reconciliation workflow.
                  properties:
                    exposed:
                      description: Exposed indicates whether this pre-action's output
                        is exposed to the parent resource.
                      type: boolean
                    fixed:
                      description: Fixed indicates whether this pre-action uses fixed
                        parameters that cannot be overridden.
                      type: boolean
                    name:
                      description: Name is the name of the MiddlewareActionBaseline
                        to execute.
                      type: string
                    parameters:
                      description: |-
                        Parameters holds input parameters for the pre-action as raw JSON.
                        The schema is defined by the corresponding MiddlewareActionBaseline.
                        Example: {"backupPath": "/data/backup", "compress": true}
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
            type: object
          status:
            description: MiddlewareStatus defines the observed state of Middleware.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the middleware's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              customResources:
                description: CustomResources holds the status of the underlying custom
                  resources managed by the middleware operator.
                properties:
                  creationTimestamp:
                    description: CreationTimestamp is the time when the custom resources
                      were first created.
                    format: date-time
                    type: string
                  disaster:
                    description: Disaster holds the optional disaster recovery configuration
                      and status.
                    properties:
                      data:
                        description: Data holds the data replication configuration
                          and status.
                        properties:
                          oppositeAddress:
                            description: OppositeAddress is the address of the peer
                              cluster for bidirectional replication.
                            type: string
                          oppositeClusterId:
                            description: OppositeClusterId is the unique identifier
                              of the peer cluster.
                            type: string
                          oppositeClusterName:
                            description: OppositeClusterName is the name of the peer
                              cluster.
                            type: string
                          oppositeClusterNamespace:
                            description: OppositeClusterNamespace is the namespace
                              in the peer cluster.
                            type: string
                          phase:
                            description: Phase is the current phase of the data replication
                              process.
                            type: string
                          targetAddress:
                            description: Address is the target address for data replication.
                            type: string
                        type: object
                      gossip:
                        description: Gossip holds the gossip protocol discovery configuration
                          for cluster membership.
                        properties:
                          advertiseAddress:
                            description: AdvertiseAddress is the address this node
                              advertises to peers for gossip communication.
                            type: string
                          advertisePort:
                            description: AdvertisePort is the port this node advertises
                              to peers for gossip communication.
                            format: int64
                            type: integer
                          clusterPhase:
                            description: ClusterPhase is the current phase of the
                              cluster as a whole.
                            type: string
                          clusterRole:
                            description: ClusterRole is the role of this node within
                              the cluster (e.g., primary, secondary).
                            type: string
                          gossipPhase:
                            description: GossipPhase is the current phase of the gossip
                              subsystem specifically.
                            type: string
                          phase:
                            description: Phase is the current phase of the gossip
                              protocol.
                            type: string
                          role:
                            description: Role is the replication role of this node.
                            type: string
                        type: object
                    type: object
                  include:
                    description: Include holds references to sub-resources (pods,
                      PVCs, services, etc.) associated with this middleware.
                    properties:
                      daemonsets:
                        description: Daemonsets is the list of associated DaemonSet
                          resources.
                        items:
                          description: IncludeModel represents a reference to a Kubernetes
                            sub-resource.
                          properties:
                            name:
                              description: Name is the name of the sub-resource.
                              type: string
                            source:
                              description: Source is the origin of this sub-resource
                                reference.
                              type: string
                            sourceName:
                              description: SourceName is the name of the source that
                                created this sub-resource.
                              type: string
                            type:
                              description: Type is the kind of the sub-resource.
                              type: string
                          type: object
                        type: array
                      deployments:
                        description: Deployments is the list of associated Deployment
                          resources.
                        items:
                          description: IncludeModel represents a reference to a Kubernetes
                            sub-resource.
                          properties:
                            name:
                              description: Name is the name of the sub-resource.
                              type: string
                            source:
                              description: Source is the origin of this sub-resource
                                reference.
                              type: string
                            sourceName:
                              description: SourceName is the name of the source that
                                created this sub-resource.
                              type: string
                            type:
                              description: Type is the kind of the sub-resource.
                              type: string
                          type: object
                        type: array
                      pods:
                        description: Pods is the list of associated Pod resources.
                        items:
                          description: IncludeModel represents a reference to a Kubernetes
                            sub-resource.
                          properties:
                            name:
                              description: Name is the name of the sub-resource.
                              type: string
                            source:
                              description: Source is the origin of this sub-resource
                                reference.
                              type: string
                            sourceName:
                              description: SourceName is the name of the source that
                                created this sub-resource.
                              type: string
                            type:
                              description: Type is the kind of the sub-resource.
                              type: string
                          type: object
                        type: array
                      pvcs:
                        description: Pvcs is the list of associated PersistentVolumeClaim
                          resources.
                        items:
                          description: IncludeModel represents a reference to a Kubernetes
                            sub-resource.
                          properties:
                            name:
                              description: Name is the name of the sub-resource.
                              type: string
                            source:
                              description: Source is the origin of this sub-resource
                                reference.
                              type: string
                            sourceName:
                              description: SourceName is the name of the source that
                                created this sub-resource.
                              type: string
                            type:
                              description: Type is the kind of the sub-resource.
                              type: string
                          type: object
                        type: array
                      services:
                        description: Services is the list of associated Service resources.
                        items:
                          description: IncludeModel represents a reference to a Kubernetes
                            sub-resource.
                          properties:
                            name:
                              description: Name is the name of the sub-resource.
                              type: string
                            source:
                              description: Source is the origin of this sub-resource
                                reference.
                              type: string
                            sourceName:
                              description: SourceName is the name of the source that
                                created this sub-resource.
                              type: string
                            type:
                              description: Type is the kind of the sub-resource.
                              type: string
                          type: object
                        type: array
                      statefulsets:
                        description: Statefulsets is the list of associated StatefulSet
                          resources.
                        items:
                          description: IncludeModel represents a reference to a Kubernetes
                            sub-resource.
                          properties:
                            name:
                              description: Name is the name of the sub-resource.
                              type: string
                            source:
                              description: Source is the origin of this sub-resource
                                reference.
                              type: string
                            sourceName:
                              description: SourceName is the name of the source that
                                created this sub-resource.
                              type: string
                            type:
                              description: Type is the kind of the sub-resource.
                              type: string
                          type: object
                        type: array
                    type: object
                  phase:
                    description: Phase is the current lifecycle phase of the custom
                      resources.
                    type: string
                  reason:
                    description: Reason provides a human-readable explanation of the
                      current phase.
                    type: string
                  replicas:
                    description: Replicas is the number of replicas reported by the
                      custom resources.
                    type: integer
                  resources:
                    description: Resources holds the compute resource requirements
                      (CPU, memory) of the custom resources.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  type:
                    description: Type is the kind identifier of the custom resource
                      (e.g., "cluster", "resources").
                    type: string
                type: object
              observedGeneration:
                description: The generation observed by the deployment controller.
                format: int64
                type: integer
              reason:
                description: Reason provides a human-readable explanation of the current
                  state.
                type: string
              state:
                description: 'State is the high-level state of the middleware. Valid
                  values: Available, Unavailable, Updating.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
