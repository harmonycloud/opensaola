---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: middlewarebaselines.middleware.cn
spec:
  group: middleware.cn
  names:
    kind: MiddlewareBaseline
    listKind: MiddlewareBaselineList
    plural: middlewarebaselines
    shortNames:
    - mb
    singular: middlewarebaseline
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels['middleware\.cn/component']
      name: Type
      type: string
    - jsonPath: .metadata.labels['middleware\.cn/packagename']
      name: Package
      type: string
    - jsonPath: .status.state
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          MiddlewareBaseline is the Schema for the middlewarebaselines API.
          It is a cluster-scoped resource that provides default templates for Middleware instances.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              MiddlewareBaselineSpec defines the desired state of MiddlewareBaseline.
              It serves as the cluster-scoped default template for creating Middleware instances.
            properties:
              configurations:
                description: Configurations is the list of default configuration resources
                  included in this baseline.
                items:
                  description: Configuration defines a named configuration with raw
                    values.
                  properties:
                    name:
                      description: Name is the unique name identifying this configuration
                        entry.
                      type: string
                    values:
                      description: |-
                        Values holds configuration values as raw JSON, rendered from CUE/Go templates.
                        The structure depends on the Configuration name and target resource type.
                        For ConfigMap targets, this typically contains key-value pairs; for RBAC targets,
                        it contains role/binding specifications.
                        Example (ConfigMap): {"my.cnf": "max_connections=200\ninnodb_buffer_pool_size=1G"}
                        Example (ServiceAccount): {"automountServiceAccountToken": true}
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
              gvk:
                description: GVK defines the GroupVersionKind of the custom resource
                  that this baseline produces.
                properties:
                  group:
                    description: Group is the API group of the Kubernetes resource.
                    type: string
                  kind:
                    description: Kind is the kind of the Kubernetes resource.
                    type: string
                  name:
                    description: Name is the name identifier for this GVK entry.
                    type: string
                  version:
                    description: Version is the API version of the Kubernetes resource.
                    type: string
                type: object
              necessary:
                description: Necessary holds required default parameters (e.g., image)
                  for this middleware type.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              operatorBaseline:
                description: OperatorBaseline references the MiddlewareOperatorBaseline
                  that manages the operator for this middleware type.
                properties:
                  gvkName:
                    description: GvkName is the GroupVersionKind identifier used to
                      match the operator baseline.
                    type: string
                  name:
                    description: Name is the name of the MiddlewareOperatorBaseline
                      resource.
                    type: string
                type: object
              parameters:
                description: Parameters holds optional default parameters that Middleware
                  instances inherit.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              preActions:
                description: PreActions is the list of pre-actions to execute before
                  the main reconciliation workflow.
                items:
                  description: PreAction defines a pre-action to execute before the
                    main reconciliation workflow.
                  properties:
                    exposed:
                      description: Exposed indicates whether this pre-action's output
                        is exposed to the parent resource.
                      type: boolean
                    fixed:
                      description: Fixed indicates whether this pre-action uses fixed
                        parameters that cannot be overridden.
                      type: boolean
                    name:
                      description: Name is the name of the MiddlewareActionBaseline
                        to execute.
                      type: string
                    parameters:
                      description: |-
                        Parameters holds input parameters for the pre-action as raw JSON.
                        The schema is defined by the corresponding MiddlewareActionBaseline.
                        Example: {"backupPath": "/data/backup", "compress": true}
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
            type: object
          status:
            description: MiddlewareBaselineStatus defines the observed state of MiddlewareBaseline.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the baseline's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: The generation observed by the deployment controller.
                format: int64
                type: integer
              state:
                description: 'State is the high-level state of the baseline. Valid
                  values: Available, Unavailable, Updating.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
