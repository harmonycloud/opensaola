---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: middlewareoperators.middleware.cn
spec:
  group: middleware.cn
  names:
    kind: MiddlewareOperator
    listKind: MiddlewareOperatorList
    plural: middlewareoperators
    shortNames:
    - mo
    singular: middlewareoperator
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.operatorAvailable
      name: Avail
      type: string
    - jsonPath: .spec.baseline
      name: Baseline
      type: string
    - jsonPath: .metadata.labels['middleware\.cn/component']
      name: Type
      type: string
    - jsonPath: .metadata.labels['middleware\.cn/packagename']
      name: Package
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.runtime
      name: Runtime
      priority: 1
      type: string
    - jsonPath: .status.reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .status.observedGeneration
      name: ObsGen
      priority: 1
      type: integer
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          MiddlewareOperator is the Schema for the middlewareoperators API.
          It represents a deployed middleware operator (e.g., MySQL Operator) that manages Middleware instances.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MiddlewareOperatorSpec defines the desired state of MiddlewareOperator.
            properties:
              baseline:
                description: Baseline is the name of the MiddlewareOperatorBaseline
                  to use as the default template.
                type: string
              configurations:
                description: Configurations is the list of additional configuration
                  resources to create alongside the operator.
                items:
                  description: Configuration defines a named configuration with raw
                    values.
                  properties:
                    name:
                      description: Name is the unique name identifying this configuration
                        entry.
                      type: string
                    values:
                      description: |-
                        Values holds configuration values as raw JSON, rendered from CUE/Go templates.
                        The structure depends on the Configuration name and target resource type.
                        For ConfigMap targets, this typically contains key-value pairs; for RBAC targets,
                        it contains role/binding specifications.
                        Example (ConfigMap): {"my.cnf": "max_connections=200\ninnodb_buffer_pool_size=1G"}
                        Example (ServiceAccount): {"automountServiceAccountToken": true}
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
              deployment:
                description: |-
                  Deployment holds the operator Deployment specification as raw JSON.
                  Values are merged with defaults from the MiddlewareOperatorBaseline during reconciliation.
                  Common fields include image, replicas, resource limits, and environment variables.
                  Example: {"image": "mysql-operator:0.6.3", "replicas": 1, "resources": {"limits": {"cpu": "500m"}}}
                type: object
                x-kubernetes-preserve-unknown-fields: true
              globe:
                description: |-
                  Globe holds global configuration for the operator as raw JSON.
                  Typically includes image repository overrides and other cluster-wide settings.
                  These values are available to all middleware instances managed by this operator.
                  Example: {"repository": "registry.example.com/middleware"}
                type: object
                x-kubernetes-preserve-unknown-fields: true
              permissionScope:
                description: PermissionScope defines whether RBAC resources are created
                  at Cluster or Namespace scope.
                type: string
              permissions:
                description: Permissions is the list of RBAC permission definitions
                  for the operator's service accounts.
                items:
                  description: Permission defines the RBAC permission for a service
                    account.
                  properties:
                    rules:
                      description: Rules is the list of RBAC policy rules to apply
                        to the service account.
                      items:
                        description: |-
                          PolicyRule holds information that describes a policy rule, but does not contain information
                          about who the rule applies to or which namespace the rule applies to.
                        properties:
                          apiGroups:
                            description: |-
                              APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                              the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          nonResourceURLs:
                            description: |-
                              NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                              Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                              Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          resourceNames:
                            description: ResourceNames is an optional white list of
                              names that the rule applies to.  An empty set means
                              that everything is allowed.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          resources:
                            description: Resources is a list of resources this rule
                              applies to. '*' represents all resources.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          verbs:
                            description: Verbs is a list of Verbs that apply to ALL
                              the ResourceKinds contained in this rule. '*' represents
                              all verbs.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - verbs
                        type: object
                      type: array
                    serviceAccountName:
                      description: ServiceAccountName is the name of the service account
                        to bind these RBAC rules to.
                      type: string
                  type: object
                type: array
              preActions:
                description: PreActions is the list of pre-actions to execute before
                  the main operator reconciliation.
                items:
                  description: PreAction defines a pre-action to execute before the
                    main reconciliation workflow.
                  properties:
                    exposed:
                      description: Exposed indicates whether this pre-action's output
                        is exposed to the parent resource.
                      type: boolean
                    fixed:
                      description: Fixed indicates whether this pre-action uses fixed
                        parameters that cannot be overridden.
                      type: boolean
                    name:
                      description: Name is the name of the MiddlewareActionBaseline
                        to execute.
                      type: string
                    parameters:
                      description: |-
                        Parameters holds input parameters for the pre-action as raw JSON.
                        The schema is defined by the corresponding MiddlewareActionBaseline.
                        Example: {"backupPath": "/data/backup", "compress": true}
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
            type: object
          status:
            description: MiddlewareOperatorStatus defines the observed state of MiddlewareOperator.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the operator's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: The generation observed by the deployment controller.
                format: int64
                type: integer
              operatorAvailable:
                description: OperatorAvailable is a human-readable summary of operator
                  availability (e.g., "1/1").
                type: string
              operators:
                additionalProperties:
                  description: DeploymentStatus is the most recently observed status
                    of the Deployment.
                  properties:
                    availableReplicas:
                      description: Total number of available non-terminating pods
                        (ready for at least minReadySeconds) targeted by this deployment.
                      format: int32
                      type: integer
                    collisionCount:
                      description: |-
                        Count of hash collisions for the Deployment. The Deployment controller uses this
                        field as a collision avoidance mechanism when it needs to create the name for the
                        newest ReplicaSet.
                      format: int32
                      type: integer
                    conditions:
                      description: Represents the latest available observations of
                        a deployment's current state.
                      items:
                        description: DeploymentCondition describes the state of a
                          deployment at a certain point.
                        properties:
                          lastTransitionTime:
                            description: Last time the condition transitioned from
                              one status to another.
                            format: date-time
                            type: string
                          lastUpdateTime:
                            description: The last time this condition was updated.
                            format: date-time
                            type: string
                          message:
                            description: A human readable message indicating details
                              about the transition.
                            type: string
                          reason:
                            description: The reason for the condition's last transition.
                            type: string
                          status:
                            description: Status of the condition, one of True, False,
                              Unknown.
                            type: string
                          type:
                            description: Type of deployment condition.
                            type: string
                        required:
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    observedGeneration:
                      description: The generation observed by the deployment controller.
                      format: int64
                      type: integer
                    readyReplicas:
                      description: Total number of non-terminating pods targeted by
                        this Deployment with a Ready Condition.
                      format: int32
                      type: integer
                    replicas:
                      description: Total number of non-terminating pods targeted by
                        this deployment (their labels match the selector).
                      format: int32
                      type: integer
                    terminatingReplicas:
                      description: |-
                        Total number of terminating pods targeted by this deployment. Terminating pods have a non-null
                        .metadata.deletionTimestamp and have not yet reached the Failed or Succeeded .status.phase.

                        This is a beta field and requires enabling DeploymentReplicaSetTerminatingReplicas feature (enabled by default).
                      format: int32
                      type: integer
                    unavailableReplicas:
                      description: |-
                        Total number of unavailable pods targeted by this deployment. This is the total number of
                        pods that are still required for the deployment to have 100% available capacity. They may
                        either be pods that are running but not yet available or pods that still have not been created.
                      format: int32
                      type: integer
                    updatedReplicas:
                      description: Total number of non-terminating pods targeted by
                        this deployment that have the desired template spec.
                      format: int32
                      type: integer
                  type: object
                description: OperatorStatus holds the deployment status for each operator
                  deployment managed by this resource.
                type: object
              ready:
                description: Ready indicates whether all operator deployments are
                  available and healthy.
                type: boolean
              reason:
                description: Reason provides a human-readable explanation of the current
                  state.
                type: string
              runtime:
                description: Runtime is an optional summary field for kubectl display
                  (forward compatible).
                type: string
              state:
                description: 'State is the high-level state of the operator. Valid
                  values: Available, Unavailable, Updating.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
