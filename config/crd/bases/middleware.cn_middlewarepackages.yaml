---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: middlewarepackages.middleware.cn
spec:
  group: middleware.cn
  names:
    kind: MiddlewarePackage
    listKind: MiddlewarePackageList
    plural: middlewarepackages
    shortNames:
    - mp
    singular: middlewarepackage
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .status.state
      name: Status
      type: string
    - jsonPath: .metadata.labels['middleware\.cn/component']
      name: Type
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          MiddlewarePackage is the Schema for the middlewarepackages API.
          It is a cluster-scoped resource that represents an installable middleware distribution package.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              MiddlewarePackageSpec defines the desired state of MiddlewarePackage.
              It describes the metadata and contents of a middleware distribution package.
            properties:
              catalog:
                description: Catalog lists the resources included in this package
                  (CRDs, baselines, configurations, actions).
                properties:
                  actions:
                    description: Actions is the list of MiddlewareActionBaseline resource
                      names in this package.
                    items:
                      type: string
                    type: array
                  configurations:
                    description: Configurations is the list of MiddlewareConfiguration
                      resource names in this package.
                    items:
                      type: string
                    type: array
                  crds:
                    description: Crds is the list of CustomResourceDefinition names
                      bundled in this package.
                    items:
                      type: string
                    type: array
                  definitions:
                    description: Baselines is the list of baseline resource names
                      (MiddlewareBaseline and MiddlewareOperatorBaseline) in this
                      package.
                    items:
                      type: string
                    type: array
                type: object
              description:
                description: Description is a human-readable description of this middleware
                  package.
                type: string
              name:
                description: Name is the display name of the middleware package.
                type: string
              owner:
                description: Owner is the entity or team that maintains this package.
                type: string
              type:
                description: Type is the middleware type identifier (e.g., "mysql",
                  "redis", "kafka").
                type: string
              version:
                description: Version is the semantic version of this package.
                type: string
            type: object
          status:
            description: MiddlewarePackageStatus defines the observed state of MiddlewarePackage.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the package's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: The generation observed by the deployment controller.
                format: int64
                type: integer
              state:
                description: 'State is the high-level state of the package. Valid
                  values: Available, Unavailable, Updating.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
