apiVersion: middleware.cn/v1
kind: MiddlewareAction
metadata:
  labels:
    app.kubernetes.io/name: opensaola
    app.kubernetes.io/managed-by: kustomize
  name: middlewareaction-sample
spec:
  # TODO(user): Add fields here
