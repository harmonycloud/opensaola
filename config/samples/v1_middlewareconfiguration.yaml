apiVersion: middleware.cn/v1
kind: MiddlewareConfiguration
metadata:
  labels:
    app.kubernetes.io/name: opensaola
    app.kubernetes.io/managed-by: kustomize
  name: middlewareconfiguration-sample
spec:
  # TODO(user): Add fields here
