apiVersion: middleware.cn/v1
kind: MiddlewareOperator
metadata:
  labels:
    app.kubernetes.io/name: opensaola
    app.kubernetes.io/managed-by: kustomize
  name: middlewareoperator-sample
spec:
  # TODO(user): Add fields here
