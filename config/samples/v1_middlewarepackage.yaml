apiVersion: middleware.cn/v1
kind: MiddlewarePackage
metadata:
  labels:
    app.kubernetes.io/name: opensaola
    app.kubernetes.io/managed-by: kustomize
  name: middlewarepackage-sample
spec:
  # TODO(user): Add fields here
